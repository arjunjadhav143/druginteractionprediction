# druginteractionprediction
🧪 **Drug–Drug Interaction Prediction System** – A machine learning pipeline that predicts predefined drug–drug interaction types using molecular fingerprints (RDKit) and an XGBoost multiclass classifier. Handles class imbalance with SMOTE, maps SMILES data, and saves trained models for fast, reusable predictions. 
